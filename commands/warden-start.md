Use Warden to bootstrap this session.

1. Call `health_check`.
2. Call `get_status`.
3. Call `get_next_task`.
4. Summarize the current phase, blockers, and next required task.
5. State that completion must be approved by Warden's finish gate.